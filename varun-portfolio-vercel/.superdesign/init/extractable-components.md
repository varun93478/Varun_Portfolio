# Extractable components

- `StudioConcept` — full Figma-inspired Work application. Props: `comparisonMode?: boolean`, proposed `showSplash?: boolean`.
- `CaseStorySpine` — compact persistent case-study story navigation.
- `ArtifactDisclosure` — accessible expandable evidence panel.

No shared navigation extraction is needed for the new homepage because Varun OS is a new target with its own shell.
