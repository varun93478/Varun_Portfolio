# Shared component inventory

- `app/concepts/StudioExperience.tsx`: existing Figma-inspired portfolio workspace and navigation shell.
- `app/concepts/WorkspaceTabs.tsx`: browser-style project tabs used by the workspace.
- `app/concepts/SystemOrbit3D.tsx`: interactive Three.js system model.
- `app/work/case-study-story.tsx`: reusable case-study story spine, evidence, flow, IA, state and annotated-screen primitives.
- `app/concepts/PortfolioLinks.tsx`: resume and contact actions.

The new Varun OS shell should reuse `StudioConcept` as the Work application instead of duplicating its project workspace.
